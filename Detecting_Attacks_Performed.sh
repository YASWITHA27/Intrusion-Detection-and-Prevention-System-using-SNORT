# Type the following commands in the Root folder of kali Linux (Attacker Machine) and perform the folllowing attacks on victim machine (Ubuntu 20.04)  
ifconfig
nmap ip_address of victim
ping  ip_address of victim
ftp ip_address of victim
ssh ip_address of victim
terrorism (rule) (google search)
Online gaming (google search)



#Type the following commands in Ubuntu 20.04 to detect the attacks performed by the attacker machine (Kali Linux)

# Commands that has to be followed in victim machine in order to check the rules written and for detecting the attacks 

cd rules
snort –T –c /etc/snort/snort. conf
snort –A console  –c /etc/snort/snort. conf
